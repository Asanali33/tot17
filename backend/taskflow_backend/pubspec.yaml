name: taskflow_backend
description: Backend for TaskFlow app.
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  mongo_dart: ^0.10.0
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
